---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: crud-app-deployment
spec:
  selector:
    matchLabels:
      app: crud-app
  template:
    metadata:
      labels:
        app: crud-app
      annotations:
        dapr.io/enabled: 'true'
        dapr.io/app-id: crud-app
        dapr.io/app-port: '8080'
        dapr.io/config: dapr-go-crud-app
        dapr.io/sidecar-listen-addresses: 0.0.0.0
        dapr.io/log-as-json: 'true'
    spec:
      containers:
      - name: crud-app
        image: ghcr.io/andriykalashnykov/dapr-go-crud-app/app:latest
        imagePullPolicy: Always
        args:
        - serve
        - -connStr
        - dapr
        - -maxItems
        - '20'
        - -cleanupIntervalSeconds
        - '600'
        ports:
        - containerPort: 8080
        readinessProbe:
          httpGet:
            path: /api/v1/todos
            port: 8080
          initialDelaySeconds: 5
          periodSeconds: 5
          failureThreshold: 6
        livenessProbe:
          httpGet:
            path: /api/v1/todos
            port: 8080
          initialDelaySeconds: 30
          periodSeconds: 10
          failureThreshold: 3
        securityContext:
          allowPrivilegeEscalation: false
          readOnlyRootFilesystem: true
          runAsNonRoot: true
          runAsUser: 65532
          capabilities:
            drop:
            - ALL
        volumeMounts:
        - name: tmp
          mountPath: /tmp
      securityContext:
        runAsNonRoot: true
        runAsUser: 65532
        seccompProfile:
          type: RuntimeDefault
      volumes:
      - name: tmp
        emptyDir: {}
---
kind: Service
apiVersion: v1
metadata:
  name: crud-app
spec:
  selector:
    app: crud-app
  type: ClusterIP
  ports:
  - name: http
    port: 8080
    targetPort: 8080
